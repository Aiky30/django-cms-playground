from django.apps import AppConfig

"""
    The STB plugin is the most awkward plugin you can think of. It's aim is to break everything at any opportunity.
    You can never beat STB
"""


class CmsStbPluginConfig(AppConfig):
    name = 'cms_stb_plugin'
